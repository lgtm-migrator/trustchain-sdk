{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/claim/src",
  "targets": {
    "install": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "npm install --prefix packages/claim"
      }
    },
    "build": {
      "executor": "@nrwl/web:rollup",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/claim",
        "main": "packages/claim/src/index.ts",
        "tsConfig": "packages/claim/tsconfig.lib.json",
        "project": "packages/claim/package.json",
        "entryFile": "packages/claim/src/index.ts",
        "format": ["esm"],
        "buildableProjectDepsInPackageJsonType": "dependencies"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/claim/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/packages/claim"],
      "options": {
        "jestConfig": "packages/claim/jest.config.ts",
        "passWithNoTests": true,
        "runInBand": true,
        "codeCoverage": true,
        "coverageReporters": ["lcov"]
      }
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "preset": "angular",
        "trackDeps": true,
        "commitMessageFormat": "fix(${projectName}): release version ${version}",
        "postTargets": ["claim:publish", "claim:github"]
      }
    },
    "github": {
      "executor": "@jscutlery/semver:github",
      "options": {
        "tag": "${tag}",
        "notes": "${notes}"
      }
    },
    "publish": {
      "executor": "ngx-deploy-npm:deploy",
      "options": {
        "access": "public"
      }
    }
  },
  "tags": []
}
