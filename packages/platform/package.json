{
  "name": "@trustcerts/platform",
  "version": "0.0.2",
  "type": "commonjs",
  "repository": "https://github.com/trustcerts/trustchain-sdk2",
  "scripts": {
    "api": "rm -rf src/lib && mkdir src/lib && npx @openapitools/openapi-generator-cli generate -g typescript-axios -i https://platform.dev.trustcerts.de/api-json -o src/lib --additional-properties=supportsES6=true,enumPropertyNaming=original,enumNameSuffix="
  }
}
