{
  "sourceRoot": "packages/did-hash/src",
  "targets": {
    "install": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "npm install --prefix packages/did-hash"
      }
    },
    "build": {
      "executor": "@nrwl/web:rollup",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/did-hash",
        "main": "packages/did-hash/src/index.ts",
        "tsConfig": "packages/did-hash/tsconfig.lib.json",
        "project": "packages/did-hash/package.json",
        "entryFile": "packages/did-hash/src/index.ts",
        "format": ["esm", "umd"],
        "buildableProjectDepsInPackageJsonType": "dependencies"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/did-hash/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/packages/did-hash"],
      "options": {
        "jestConfig": "packages/did-hash/jest.config.ts",
        "passWithNoTests": true,
        "runInBand": true
      }
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "preset": "angular",
        "trackDeps": true,
        "commitMessageFormat": "fix(${projectName}): release version ${version}",
        "postTargets": ["did-hash:deploy"]
      }
    },
    "deploy": {
      "executor": "ngx-deploy-npm:deploy",
      "options": {
        "access": "public"
      }
    }
  },
  "tags": []
}
